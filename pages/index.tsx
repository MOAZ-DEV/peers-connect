import { UserRegist } from "@/components/forms/UserCase";

const Home = () => {

  return (
    <div className="min-h-screen flex flex-col gap-12 items-center justify-center">
      <UserRegist />
    </div>
  );
};

export default Home;
